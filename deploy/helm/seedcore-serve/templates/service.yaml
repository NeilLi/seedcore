apiVersion: v1
kind: Service
metadata:
  name: {{ include "seedcore-serve.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector: { app: {{ include "seedcore-serve.name" . }} }
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http






