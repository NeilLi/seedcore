# Optional ML / local-AI stack.
# Docker installs the CPU-only Torch/DGL runtime separately when ENABLE_ML=1.
# CUDA, bitsandbytes, and other GPU-specific packages are intentionally excluded
# from the default path.

# --- AI & NLP ---
dspy-ai==2.4.9
scikit-learn==1.4.2
datasets==2.14.7
fsspec==2023.10.0
transformers>=4.49.0,<5.0
sentence-transformers==2.7.0
tokenizers>=0.21.0
safetensors==0.4.3
peft>=0.14.0

# --- Classical / Distributed ML ---
xgboost>=2.0.3
xgboost-ray>=0.1.19
