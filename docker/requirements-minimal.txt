# Core API
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic-settings==2.1.0

# DB / drivers
asyncpg==0.29.0
psycopg[binary]==3.1.18
psycopg2-binary==2.9.9
pgvector==0.2.4
neo4j-driver==5.15.0
sqlalchemy[asyncio]==2.0.23
mysql-connector-python==8.2.0
pymysql>=1.1
aiomysql==0.2.0
cryptography>=41.0.0

# HTTP / networking
aiohttp==3.9.1
httpx==0.25.2
grpcio==1.63.0
protobuf==4.21.12

# Utilities
pyyaml==6.0.1
tqdm==4.66.1
prometheus_client==0.19.0
redis==5.0.1

# Core ML / DL side deps (Torch/DGL/NumPy are installed in Dockerfile)
torchdata==0.7.1

# Data / Tables
pandas==2.2.2
pyarrow==15.0.2
datasets==2.14.7
fsspec[http]==2023.10.0   # matches datasets constraint

# Seedcore / AI deps
pydantic==2.5.0           # required by dspy-ai
dspy-ai==2.4.4

# ML dependencies for salience scoring
scikit-learn==1.3.0

# XGBoost integration
xgboost>=2.0.0
xgboost-ray>=0.1.19

# Ray itself is already in the base image (rayproject/ray:2.33.0-py310)
# ray[default,train]==2.33.0
aiorwlock==1.3.0
aioredis==2.0.1

# LLM client
openai>=1.12.0