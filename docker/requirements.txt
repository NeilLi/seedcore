# Full SeedCore dependency set for local development or explicit full installs.
# Docker defaults to `requirements.base.txt` and only adds the optional ML layer
# when ENABLE_ML=1.

-r requirements.base.txt
-r requirements.optional-ml.txt
