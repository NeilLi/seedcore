# deny_missing_approval

Deterministic Restricted Custody Transfer scenario where:

- a required approval is missing or incomplete
- policy returns `deny`
- no execution token is minted
- a policy receipt still exists
