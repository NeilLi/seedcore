# escalate_break_glass

Deterministic Restricted Custody Transfer scenario where:

- a break-glass request exists
- strict automated allow is not appropriate
- policy returns `escalate`
- the explanation payload carries an explicit review obligation
